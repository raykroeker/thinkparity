/*
 * Generated On: ${environment.generatedOn}
 */
package ${model.packageName};

/**
 * <b>Title:</b>thinkParity ${model.name}<br>
 * <b>Description:</b>
 * 
 * @author ${environment.author}
 * @version ${model.version}
 */
${model.classModifier}class ${model.className} {

	/** Create ${model.className}. */
	public ${model.className}() { super(); }
}
